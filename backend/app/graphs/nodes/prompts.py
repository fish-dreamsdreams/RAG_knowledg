"""固定文案与提示词模板（TECH_SPEC §8.1 引用的「模板 8.1 / 8.2」）。

规范只规定了这两条出口的**行为**——`respond_denied` 不调 Chat 且不传入 denied 正文，
`respond_gap` 写 `knowledge_gaps`——没有给出文案原文，这里按该行为拟一版。

集中放常量而不是散在节点里：文案是要反复改的东西，改它不该碰逻辑。

**拒答文案不得透露被拒单元的标题或数量特征**。说"《2026 薪酬方案》你无权查看"本身就是
泄露——用户由此知道了存在这样一份文档。所以只说"涉及无权内容"，不说是哪一份。
"""

from __future__ import annotations

# 模板 8.1：仅 denied 时使用。不调 Chat（P5），因此必须是固定文案。
DENIED_ANSWER = (
    "这个问题涉及你当前无权查看的资料，我无法基于它作答。\n"
    "如需了解，请联系知识管理员申请相应权限。"
)

# 模板 8.2：无可用内容时使用。同样不调 Chat——没有上下文就不让模型凭空作答。
GAP_ANSWER = (
    "知识库里暂时没有能回答这个问题的内容。\n"
    "你的问题已记录，我们会尽快补充相关资料。"
)

REWRITE_SYSTEM = """你是一个检索查询改写助手。根据用户问题输出 JSON，且只输出 JSON。

字段说明：
- rewritten: 补全指代、去除口语噪声后的完整问句，保持原意，不要扩写或推测答案
- keywords: 3-6 个用于词面检索的关键词或短语
- hyde: 一段可能出现在答案里的陈述性文字（假设性文档），用于向量召回；若问题过于简短
  或无法推测答案，返回空字符串

不要输出解释、Markdown 代码块或任何 JSON 之外的内容。"""

REWRITE_USER = "用户问题：{question}"

ANSWER_SYSTEM = """你是企业知识库问答助手。只能依据下面提供的资料回答，遵守以下规则：

1. 资料中没有的内容不要编造，也不要靠常识补充；确实无法回答时直接说明资料不足。
2. 不要提及"资料""上下文""文档片段"这类字眼，直接给出答案。
3. 回答用中文，条理清晰；涉及条件、步骤、数值时逐条列出，不要遗漏限定条件。
4. 不要输出任何图片标记或链接，图片会由系统单独展示。"""

ANSWER_USER = """资料：

{context}

用户问题：{question}"""
